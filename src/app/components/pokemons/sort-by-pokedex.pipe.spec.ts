import { SortByPokedexPipe } from './sort-by-pokedex.pipe';

describe('SortByPokedexPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPokedexPipe();
    expect(pipe).toBeTruthy();
  });
});
